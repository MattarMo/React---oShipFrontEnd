Switch(
    whichFilter,
    SS_CurrentAgendaBut,
    SortByColumns(
        ShowColumns(
            Filter(
                PowerAppTestList,
                StartsWith(
                    Initiative_x0020_Representative_,
                    textInput.Text
                ) ||
                StartsWith(
                    InitiativeName,
                    textInput.Text
                ) || StartsWith(
                    TRAC_x0020_ID,
                    textInput.Text
                ),
                DateConversion >= Value(Text(todayDate, "[$-en-US]yyyymmdd")) && DateConversion < Value(Text(todayDate + 7, "[$-en-US]yyyymmdd"))
            ),
            "ID",
            "tracidtest",
            "Initiative_x0020_Representative_",
            "DateConversion",
            "TRAC_x0020_ID",
            "TracMeetingDate",
            "InitiativeName",
            "InitiativeRepresentative",
            "Reason_for_Appearance",
            "InitiativeScope",
            "IT_Manager"
        ),
        "TracMeetingDate",
        If(
            SortDescending1,
            Ascending,
            Descending
        ),
        "tracidtest",
        If(
            SortDescending2,
            Descending,
            Ascending
        ),
        "InitiativeName",
        If(
            SortDescending3,
            Ascending,
            Descending
        ),
        "InitiativeScope",
        If(
            SortDescending4,
            Ascending,
            Descending
        )
    ),
    SS_AllTracBut,
    SortByColumns(
        ShowColumns(
            Filter(
                PowerAppTestList,
                StartsWith(
                    Initiative_x0020_Representative_,
                    textInput.Text
                ) || StartsWith(
                    InitiativeName,
                    textInput.Text
                ) || StartsWith(
                    TRAC_x0020_ID,
                    textInput.Text
                )
            ),
            "ID",
            "tracidtest",
            "Initiative_x0020_Representative_",
            "DateConversion",
            "TRAC_x0020_ID",
            "TracMeetingDate",
            "InitiativeName",
            "InitiativeRepresentative",
            "Reason_for_Appearance",
            "InitiativeScope",
            "IT_Manager"
        ),
        "TracMeetingDate",
        If(
            SortDescending1,
            Ascending,
            Descending
        ),
        "tracidtest",
        If(
            SortDescending2,
            Descending,
            Ascending
        ),
        "InitiativeName",
        If(
            SortDescending3,
            Ascending,
            Descending
        ),
        "InitiativeScope",
        If(
            SortDescending4,
            Ascending,
            Descending
        )
    ),
    SS_initiativesBut,
    SortByColumns(
        ShowColumns(
            Filter(
                PowerAppTestList,
                StartsWith(
                    Initiative_x0020_Representative_,
                    textInput.Text
                ) || StartsWith(
                    InitiativeName,
                    textInput.Text
                ) || StartsWith(
                    TRAC_x0020_ID,
                    textInput.Text
                ),
                Initiative_x0020_Representative_ = currentUser
            ),
            "ID",
            "tracidtest",
            "TRAC_x0020_ID",
            "Initiative_x0020_Representative_",
            "DateConversion",
            "TracMeetingDate",
            "InitiativeName",
            "InitiativeRepresentative",
            "Reason_for_Appearance",
            "InitiativeScope",
            "IT_Manager"
        ),
        "TracMeetingDate",
        If(
            SortDescending1,
            Ascending,
            Descending
        ),
        "tracidtest",
        If(
            SortDescending2,
            Descending,
            Ascending
        ),
        "InitiativeName",
        If(
            SortDescending3,
            Ascending,
            Descending
        ),
        "InitiativeScope",
        If(
            SortDescending4,
            Ascending,
            Descending
        )
    ),
    SS_NextTracBut,
    SortByColumns(
        ShowColumns(
            Filter(
                PowerAppTestList,
                StartsWith(
                    Initiative_x0020_Representative_,
                    textInput.Text
                ) || StartsWith(
                    InitiativeName,
                    textInput.Text
                ) || StartsWith(
                    TRAC_x0020_ID,
                    textInput.Text
                ),
                DateConversion >Value(Text(todayDate + 6, "[$-en-US]yyyymmdd")) && DateConversion <= Value(Text(todayDate + 15, "[$-en-US]yyyymmdd"))
            ),
            "ID",
            "Initiative_x0020_Representative_",
            "DateConversion",
            "tracidtest",
            "TRAC_x0020_ID",
            "TracMeetingDate",
            "InitiativeName",
            "InitiativeRepresentative",
            "Reason_for_Appearance",
            "InitiativeScope",
            "IT_Manager"
        ),
        "TracMeetingDate",
        If(
            SortDescending1,
            Ascending,
            Descending
        ),
        "tracidtest",
        If(
            SortDescending2,
            Descending,
            Ascending
        ),
        "InitiativeName",
        If(
            SortDescending3,
            Ascending,
            Descending
        ),
        "InitiativeScope",
        If(
            SortDescending4,
            Ascending,
            Descending
        )
    ),
    NoBut_1,
    SortByColumns(
        ShowColumns(
            Filter(
                PowerAppTestList,
                StartsWith(
                    Initiative_x0020_Representative_,
                    textInput.Text
                ) || StartsWith(
                    InitiativeName,
                    textInput.Text
                ) || StartsWith(
                    TRAC_x0020_ID,
                    textInput.Text
                ),
                DateConversion < Value(Text(todayDate, "[$-en-US]yyyymmdd"))
            ),
            "ID",
            "tracidtest",
            "Initiative_x0020_Representative_",
            "DateConversion",
            "TRAC_x0020_ID",
            "TracMeetingDate",
            "InitiativeName",
            "InitiativeRepresentative",
            "Reason_for_Appearance",
            "InitiativeScope",
            "IT_Manager"
        ),
        "TracMeetingDate",
        If(
            SortDescending1,
            Ascending,
            Descending
        ),
        "tracidtest",
        If(
            SortDescending2,
            Descending,
            Ascending
        ),
        "InitiativeName",
        If(
            SortDescending3,
            Ascending,
            Descending
        ),
        "InitiativeScope",
        If(
            SortDescending4,
            Ascending,
            Descending
        )
    ),
    PowerAppTestList
)


 ClearCollect(
        list_1,
        Filter(
            SortByColumns(
                PowerAppTestList,
                "ID",
                Ascending
            ),
            ID > 0
        )
    ),
    ClearCollect(
        list_2,
        Filter(
            SortByColumns(
                PowerAppTestList,
                "ID",
                Descending
            ),
            ID > 0
        )
    ),
    ClearCollect(
        list_t,
        Filter(
            SortByColumns(
                'Trac Reviewers Comments',
                "ID",
                Descending
            ),
            ID > 0
        )

    ClearCollect(
    listAll,
    list_1,
    Filter(
        list_2,
        Not(ID in list_1.ID)
    )
);
Clear(list_1);
Clear(list_2)

 Concurrent(
 ClearCollect(
        list_1,
        Filter(
            SortByColumns(
                PowerAppTestList,
                "ID",
                Ascending
            ),
            ID > 0
        )
    ),
    ClearCollect(
        list_2,
        Filter(
            SortByColumns(
                PowerAppTestList,
                "ID",
                Descending
            ),
            ID > 0
        )
    )
 );
 ClearCollect(
    listAll,
    list_1,
    Filter(
        list_2,
        Not(ID in list_1.ID)
    )
);
Clear(list_1);
Clear(list_2)
;ITSTRAC_Account@dteenergy.com;raghavendra.thammala@dteenergy.com;teresa.ayala-fabian@dteenergy.com;
